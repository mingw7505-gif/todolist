# TodoList 网站产品需求文档 PRD

## 1. 产品目标

TodoList 网站的目标是帮助用户快速记录、管理和完成日常待办事项。

这个产品不追求复杂的项目管理能力，而是提供一个简单、清晰、易上手的任务清单工具，让用户打开网页后可以立即新增任务、查看任务、标记完成，并保持对当天或近期事项的掌控。

核心目标：

- 让用户用最少步骤创建待办事项。
- 让用户清楚知道哪些任务未完成、哪些任务已完成。
- 让用户可以快速整理、修改和删除任务。
- 提供一个适合作为 MVP 的轻量级网站版本，方便后续逐步扩展。

## 2. 用户场景

### 场景一：记录临时待办

用户突然想到一件要做的事情，例如“晚上买牛奶”或“明天回复客户邮件”，希望马上记录下来，避免遗忘。

用户需要：

- 快速输入任务内容。
- 点击一次即可保存。
- 保存后可以马上在列表中看到任务。

### 场景二：查看今天要做什么

用户打开网站，希望快速查看当前还没有完成的任务。

用户需要：

- 未完成任务清晰展示。
- 已完成任务和未完成任务有明显区别。
- 可以快速判断任务数量和状态。

### 场景三：完成任务后打勾

用户完成一件事情后，希望把它标记为完成。

用户需要：

- 点击任务前面的勾选框或完成按钮。
- 任务状态立即变为已完成。
- 已完成任务在视觉上弱化，例如文字划线或颜色变浅。

### 场景四：修改写错的任务

用户发现任务内容写错了，或者需要补充说明。

用户需要：

- 可以编辑已有任务。
- 修改后保存并更新列表。
- 如果不想修改，可以取消编辑。

### 场景五：删除不需要的任务

用户不再需要某条任务，希望从列表中移除。

用户需要：

- 可以删除单条任务。
- 删除前最好有简单确认，避免误删。

## 3. 核心功能

### 3.1 新增任务

用户可以通过输入框创建新的待办任务。

功能要求：

- 页面顶部或主要区域提供任务输入框。
- 输入框旁边提供“添加”按钮。
- 用户输入内容后点击按钮，任务加入列表。
- 如果输入为空，不能添加任务，并提示用户输入内容。
- 添加成功后，输入框自动清空。

### 3.2 任务列表

系统展示用户创建的所有任务。

功能要求：

- 每条任务展示任务名称。
- 每条任务展示当前状态：未完成或已完成。
- 新增任务默认是未完成状态。
- 未完成任务和已完成任务应有明显视觉区分。

### 3.3 标记完成 / 取消完成

用户可以切换任务完成状态。

功能要求：

- 每条任务前有勾选框。
- 勾选后任务变为已完成。
- 取消勾选后任务恢复为未完成。
- 已完成任务的文字可以显示删除线或变浅。

### 3.4 编辑任务

用户可以修改已有任务内容。

功能要求：

- 每条任务提供编辑入口。
- 点击编辑后，任务文字变成可输入状态。
- 用户可以保存修改。
- 用户可以取消编辑。
- 修改后的任务不能为空。

### 3.5 删除任务

用户可以删除不需要的任务。

功能要求：

- 每条任务提供删除按钮。
- 点击删除后，从列表中移除任务。
- MVP 阶段可以使用浏览器默认确认弹窗，也可以直接删除。

### 3.6 任务筛选

用户可以根据状态筛选任务。

功能要求：

- 提供三个筛选选项：全部、未完成、已完成。
- 默认展示全部任务。
- 切换筛选后，列表只显示对应状态的任务。

### 3.7 本地保存

用户刷新页面后，任务不应丢失。

功能要求：

- MVP 阶段使用浏览器本地存储保存任务。
- 用户刷新页面后，任务列表仍然保留。
- 不需要登录账号。
- 不需要后端数据库。

## 4. 页面结构

TodoList 网站 MVP 只需要一个主页面。

### 4.1 页面整体布局

页面建议结构：

- 顶部标题区
- 新增任务输入区
- 任务状态筛选区
- 任务列表区
- 底部统计区

### 4.2 顶部标题区

内容：

- 网站名称，例如“TodoList”
- 简短副标题，例如“管理今天要完成的事”

目的：

- 告诉用户当前网站用途。
- 建立清晰、简单的产品感。

### 4.3 新增任务输入区

内容：

- 任务输入框
- 添加按钮

目的：

- 作为页面最重要的操作入口。
- 用户打开页面后可以直接创建任务。

### 4.4 任务状态筛选区

内容：

- 全部
- 未完成
- 已完成

目的：

- 帮助用户快速查看不同状态的任务。

### 4.5 任务列表区

内容：

- 勾选框
- 任务名称
- 编辑按钮
- 删除按钮

目的：

- 展示所有任务。
- 支持用户对任务进行完成、编辑、删除操作。

### 4.6 底部统计区

内容：

- 当前总任务数
- 未完成任务数
- 已完成任务数

目的：

- 让用户快速了解任务整体状态。

## 5. 交互流程

### 5.1 新增任务流程

1. 用户在输入框中输入任务内容。
2. 用户点击“添加”按钮。
3. 系统判断输入内容是否为空。
4. 如果为空，提示用户“请输入任务内容”。
5. 如果不为空，系统创建一条未完成任务。
6. 新任务显示在任务列表中。
7. 输入框清空，方便继续添加。

### 5.2 完成任务流程

1. 用户看到任务列表。
2. 用户点击某条任务前面的勾选框。
3. 系统将任务状态改为已完成。
4. 任务文字显示为已完成样式。
5. 底部统计数量同步更新。

### 5.3 取消完成流程

1. 用户点击已完成任务前面的勾选框。
2. 系统将任务状态改回未完成。
3. 任务文字恢复普通样式。
4. 底部统计数量同步更新。

### 5.4 编辑任务流程

1. 用户点击某条任务的编辑按钮。
2. 当前任务内容变成输入框。
3. 用户修改任务内容。
4. 用户点击保存。
5. 系统判断修改后的内容是否为空。
6. 如果为空，提示用户“任务内容不能为空”。
7. 如果不为空，保存修改并恢复普通展示状态。

### 5.5 删除任务流程

1. 用户点击某条任务的删除按钮。
2. 系统提示用户是否确认删除。
3. 用户确认后，系统删除该任务。
4. 任务列表和统计数量同步更新。

### 5.6 筛选任务流程

1. 用户点击“全部 / 未完成 / 已完成”中的一个选项。
2. 系统根据所选状态过滤任务列表。
3. 当前选中的筛选项需要有明显高亮状态。

## 6. MVP 版本范围

MVP 版本只做最核心、最容易验证的功能。

### MVP 必须包含

- 单页面 TodoList 网站。
- 新增任务。
- 展示任务列表。
- 标记完成和取消完成。
- 编辑任务。
- 删除任务。
- 按状态筛选任务。
- 显示任务统计数量。
- 使用浏览器本地存储保存任务。
- 适配桌面端和手机端基础显示。

### MVP 不包含

- 用户注册和登录。
- 多设备同步。
- 后端数据库。
- 团队协作。
- 任务提醒通知。
- 日历视图。
- 子任务。
- 文件上传。
- 复杂标签系统。

## 7. 后续可扩展功能

后续版本可以在 MVP 基础上逐步增加功能。

### 7.1 任务优先级

支持给任务设置优先级，例如高、中、低。

价值：

- 帮助用户区分重要任务。
- 优先处理更紧急的事情。

### 7.2 截止日期

支持给任务设置截止日期。

价值：

- 帮助用户管理有时间要求的任务。
- 后续可以扩展今日任务、逾期任务等视图。

### 7.3 分类或标签

支持给任务添加分类或标签，例如工作、生活、学习。

价值：

- 帮助任务较多的用户整理内容。
- 提高查找效率。

### 7.4 搜索任务

支持根据关键词搜索任务。

价值：

- 当任务数量变多时，用户可以快速找到目标任务。

### 7.5 批量操作

支持批量完成、批量删除已完成任务。

价值：

- 提高整理任务列表的效率。

### 7.6 账号和云同步

支持用户登录后在不同设备之间同步任务。

价值：

- 用户可以在电脑和手机之间继续使用同一份任务列表。

### 7.7 提醒通知

支持任务到期前提醒用户。

价值：

- 减少用户遗忘重要任务的可能性。

### 7.8 数据导出

支持导出任务数据，例如导出为 CSV。

价值：

- 方便用户备份或迁移数据。

## 给 Codex 的开发说明

请基于以上 PRD 开发一个简单、可用、单页面的 TodoList 网站。

建议优先实现 MVP，不要一开始加入登录、数据库、云同步等复杂能力。

推荐技术实现：

- 前端页面：HTML、CSS、JavaScript，或使用 React。
- 数据保存：浏览器 LocalStorage。
- 页面风格：简洁、清晰、适合日常使用。
- 适配要求：桌面端和手机端都能正常使用。

验收标准：

- 用户可以新增、完成、取消完成、编辑、删除任务。
- 用户可以按全部、未完成、已完成筛选任务。
- 刷新页面后，任务仍然存在。
- 空任务不能被添加。
- 页面在手机和电脑上都能正常显示。
